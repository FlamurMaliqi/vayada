server {
  listen 80;
  server_name _;

  location / {
    limit_except GET HEAD { deny all; }
    proxy_pass http://minio:9000/vayada-media-local/public/;
    proxy_set_header Host minio:9000;
  }
}
