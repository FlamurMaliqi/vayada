# All variables are optional in dev. Code defaults to portless URLs
# (https://api.*.localhost). Set these only if you want to override
# (e.g. plain-port dev without portless, or staging endpoints).

# Legacy rollout setting retained for deployment compatibility. First-party
# browser auth uses the relative /auth gateway below, while ordinary product
# requests use NEXT_PUBLIC_API_URL.
#   portless default:  https://api.localhost
#   plain-port dev:    http://localhost:8003
# NEXT_PUBLIC_AUTH_API_URL=

# Server-only first-party /auth gateway settings. NEXT_PUBLIC_AUTH_API_URL
# stays on apps/api for ordinary product requests.
#   portless public origin: https://affiliate.localhost
#   plain-port public origin: http://localhost:3005
# AUTH_PUBLIC_ORIGIN=
# AUTH_GATEWAY_UPSTREAM_ORIGIN=http://127.0.0.1:8003

# Target API (apps/api) — affiliate dashboard, marketplace/affiliate, finance.
#   portless default:  https://api.localhost
#   plain-port dev:    http://localhost:8003
# NEXT_PUBLIC_API_URL=

# Public-facing host for affiliate referral links
# (e.g. {hotelSlug}.{SITE_HOST}?ref={code}). Override per environment —
# in staging this should point at the staging marketplace host.
NEXT_PUBLIC_SITE_HOST=vayada.com
